select description food_item, price cost
from L_FOODS

select description, price, supplier_id, product_code, menu_item, price_increase
from L_FOODS

select supplier_id
from L_FOODS

select a, b
from sec0207

select b, a
from sec0207

select FIRST_NAME, LAST_NAME
from L_EMPLOYEES 
where FIRST_NAME = 'NANCY'

select FIRST_NAME, LAST_NAME
from L_EMPLOYEES 
where EMPLOYEE_ID >= 205

select FIRST_NAME, LAST_NAME
from L_EMPLOYEES 
where EMPLOYEE_ID <> 205

select FIRST_NAME, LAST_NAME
from L_EMPLOYEES 
where EMPLOYEE_ID > 205

select FIRST_NAME, LAST_NAME
from L_EMPLOYEES 
where EMPLOYEE_ID < 205